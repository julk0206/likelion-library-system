<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  <properties resource="db/oracle.properties" />

  <settings>
    <setting name="mapUnderscoreToCamelCase" value="true" />
  </settings>

  <typeAliases>
    <package name="com.team.five.dto" />
  </typeAliases>

  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC" />
      <dataSource type="POOLED">
        <property name="driver" value="${driver}" />
        <property name="url" value="${url}" />
        <property name="username" value="${user}" />
        <property name="password" value="${pw}" />
      </dataSource>
    </environment>
  </environments>

  <mappers>
    <mapper resource="mapper/BookMapper.xml"></mapper>
    <mapper resource="mapper/UsersMapper.xml"></mapper>
    <mapper resource="mapper/ReservationMapper.xml"></mapper>
    <mapper resource="mapper/BookItem_Statement.xml" />
    <mapper resource="mapper/Rent_Statement.xml" />
    <mapper resource="./mapper/Returning_Statement.xml" />
  </mappers>
</configuration>
